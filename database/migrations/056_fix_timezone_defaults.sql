-- Padroniza created_at no fuso local: em SQLite puro não há timezone real.
-- Aqui a gente só garante que todos created_at existam e sejam datetime('now').

-- Se você já tem defaults nos CREATE TABLE, pode deixar esse arquivo vazio.
-- Mantido para consistência de numeração.
