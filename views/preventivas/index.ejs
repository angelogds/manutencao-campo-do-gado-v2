<% layout('layout') %>

<style>
  .page-head{
    display:flex;
    align-items:flex-end;
    justify-content:space-between;
    gap:12px;
    margin-bottom:14px;
  }
  .muted{ font-size:13px; color:#6b7280; margin-top:6px; }

  .card{
    background:#fff;
    border:1px solid #e5e7eb;
    border-radius:16px;
    padding:18px;
  }

  .actions{
    display:flex;
    gap:10px;
    align-items:center;
  }

  /* ✅ MESMO botão do Equipamentos (mesma cor + mesmo formato) */
  .btn-primary{
    background:#16a34a;
    color:#fff;
    border:none;
    padding:10px 16px;
    border-radius:10px;
    font-weight:700;
    cursor:pointer;
    text-decoration:none;
    display:inline-flex;
    align-items:center;
    gap:8px;
  }
  .btn-primary:hover{ background:#15803d; }

  /* Pílulas (mais leves e bonitas) */
  .pill{
    display:inline-flex;
    align-items:center;
    gap:6px;
    padding:4px 10px;
    border-radius:999px;
    font-weight:800;
    font-size:12px;
    border:1px solid #e5e7eb;
    background:#f8fafc;
    color:#0f172a;
    white-space:nowrap;
  }
  .pill-green{ background:#ecfdf5; border-color:#bbf7d0; color:#14532d; }
  .pill-gray{  background:#f1f5f9; border-color:#e2e8f0; color:#334155; }
  .pill-blue{  background:#eff6ff; border-color:#bfdbfe; color:#1d4ed8; }

  /* Botão pequeno (Ações) igual Equipamentos */
  .btn-sm{
    display:inline-flex;
    align-items:center;
    gap:6px;
    padding:8px 12px;
    border-radius:10px;
    border:1px solid #d1d5db;
    background:#fff;
    cursor:pointer;
    text-decoration:none;
    font-weight:800;
    color:#0f172a;
    white-space:nowrap;
  }
  .btn-sm:hover{
    border-color: rgba(22,163,74,.45);
    background:#ecfdf5;
  }

  .preventiva-list{
    display:grid;
    gap:12px;
  }
  .preventiva-card{
    border:1px solid #e5e7eb;
    border-radius:14px;
    padding:14px;
    background:#fff;
  }
  .preventiva-top{
    display:flex;
    align-items:flex-start;
    justify-content:space-between;
    gap:10px;
    margin-bottom:10px;
  }
  .preventiva-code{
    color:#64748b;
    font-size:18px;
    line-height:1.1;
    font-weight:800;
  }
  .preventiva-id{
    font-size:26px;
    font-weight:800;
    line-height:1;
    text-decoration:none;
    color:#0f172a;
    display:inline-block;
    margin-top:2px;
  }
  .preventiva-title{
    font-size:24px;
    line-height:1.25;
    margin:2px 0 8px;
    font-weight:800;
    overflow-wrap:anywhere;
  }
  .preventiva-equip{
    color:#0f172a;
    font-size:18px;
    margin-bottom:8px;
    font-weight:700;
  }
  .preventiva-exec{
    color:#111827;
    font-size:16px;
    margin-bottom:10px;
  }
  .preventiva-dates{
    color:#6b7280;
    font-size:14px;
    margin:10px 0;
    border-top:1px solid #eef2f7;
    padding-top:10px;
  }
  .preventiva-meta{
    display:flex;
    flex-wrap:wrap;
    gap:8px;
  }
  .preventiva-meta .btn-sm{ margin-left:auto; }
  .pill-big{
    font-size:13px;
    padding:6px 14px;
    font-weight:800;
  }
  .pill-open{ background:#fff7ed; border-color:#fed7aa; color:#9a3412; }
  .pill-closed{ background:#ecfdf5; border-color:#bbf7d0; color:#14532d; }
  .pill-info{ background:#eff6ff; border-color:#bfdbfe; color:#1d4ed8; }

  @media (max-width: 900px){
    .card{ padding:10px; }
    .page-head{
      flex-wrap:wrap;
      align-items:flex-start;
    }
    .actions{
      width:100%;
    }
    .btn-primary{
      width:100%;
      justify-content:center;
    }
    .preventiva-code{ font-size:14px; }
    .preventiva-id{ font-size:30px; }
    .preventiva-title{ font-size:24px; }
    .preventiva-equip{ font-size:18px; }
    .preventiva-exec{ font-size:16px; }
    .preventiva-dates{ font-size:14px; }
    .pill-big{ font-size:14px; }
    .btn-sm{ padding:7px 10px; }
  }
</style>

<div class="page-head">
  <div>
    <h1>Preventivas</h1>
    <div class="muted">Planos de manutenção preventiva.</div>
  </div>

  <div class="actions">
    <a class="btn-primary" href="/preventivas/nova">+ Nova Preventiva</a>
  </div>
</div>

<div class="card">
  <div class="preventiva-list">
    <% if (!lista || !lista.length) { %>
      <div class="preventiva-card" style="text-align:center; color:#6b7280;">
        Nenhuma preventiva cadastrada.
      </div>
    <% } %>

    <% (lista || []).forEach(p => { %>
      <% const statusExec = String(p.execucao_status || "").toLowerCase(); %>
      <% const statusAberta = !statusExec || statusExec === "pendente" || statusExec === "atrasada"; %>
      <% const statusLabel = statusAberta ? "ABERTA" : "FECHADA"; %>
      <% const statusClass = statusAberta ? "pill-open" : "pill-closed"; %>
      <% const equipamentoLabel = p.equipamento_nome
            ? (p.equipamento_codigo
                ? (p.equipamento_codigo + " • " + p.equipamento_nome)
                : p.equipamento_nome)
            : "-"; %>
      <% const abertura = p.execucao_data_prevista || p.created_at || "-"; %>
      <% const fechamento = p.execucao_data_executada || "-"; %>
      <article class="preventiva-card">
        <div class="preventiva-top">
          <div>
            <div class="preventiva-code">PREV #<%= p.id %></div>
            <a class="preventiva-id" href="/preventivas/<%= p.id %>">#<%= p.id %></a>
          </div>
          <span class="pill pill-big <%= statusClass %>"><%= statusLabel %></span>
        </div>

        <h3 class="preventiva-title"><%= p.titulo || "-" %></h3>
        <div class="preventiva-equip"><%= equipamentoLabel %></div>
        <div class="preventiva-exec">
          <strong>Executores:</strong> <%= p.execucao_responsavel || "-" %>
        </div>
        <div class="preventiva-dates">
          Abertura: <%= abertura %> • Fechamento: <%= fechamento %>
        </div>

        <div class="preventiva-meta">
          <span class="pill pill-big pill-info">PREVENTIVA</span>
          <span class="pill pill-big pill-gray"><%= (p.frequencia_tipo || "-").toUpperCase() %> x<%= p.frequencia_valor || 1 %></span>
          <% if (Number(p.ativo) === 1 || p.ativo === true) { %>
            <span class="pill pill-big pill-green">PLANO ATIVO</span>
          <% } else { %>
            <span class="pill pill-big pill-gray">PLANO INATIVO</span>
          <% } %>
          <a class="btn-sm" href="/preventivas/<%= p.id %>">Abrir</a>
        </div>
      </article>
    <% }) %>
  </div>
</div>
