<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title || "Manutenção Campo do Gado" %></title>
  <link rel="stylesheet" href="/css/app.css" />
</head>

<body>
  <div class="app">
    <aside class="sidebar">
      <div class="brand">
        <div class="brand-badge">CG</div>
        <div>
          <div class="brand-title">Campo do Gado</div>
          <div class="brand-sub">V2 • Gestão Industrial</div>
        </div>
      </div>

      <nav class="nav">
        <a class="nav-item <%= activeMenu === 'dashboard' ? 'active' : '' %>" href="/dashboard">Dashboard</a>
        <a class="nav-item <%= activeMenu === 'equipamentos' ? 'active' : '' %>" href="/equipamentos">Equipamentos</a>
        <a class="nav-item <%= activeMenu === 'os' ? 'active' : '' %>" href="/os">Ordens de Serviço</a>
        <a class="nav-item <%= activeMenu === 'preventivas' ? 'active' : '' %>" href="/preventivas">Preventivas</a>
        <a class="nav-item <%= activeMenu === 'compras' ? 'active' : '' %>" href="/compras">Compras</a>
        <a class="nav-item <%= activeMenu === 'estoque' ? 'active' : '' %>" href="/estoque">Estoque</a>

        <!-- ✅ Motores com badge só quando tiver ENVIADO_REBOB -->
        <a class="nav-item <%= activeMenu === 'motores' ? 'active' : '' %>" href="/motores" style="display:flex; align-items:center; gap:10px;">
          <span>Motores</span>
          <% if (motorBadgeCount && motorBadgeCount > 0) { %>
            <span style="margin-left:auto; display:inline-flex; align-items:center; justify-content:center; min-width:28px; height:22px; padding:0 8px; border-radius:999px; font-size:12px; font-weight:900; background:#fff7ed; color:#9a3412; border:1px solid #fed7aa;">
              <%= motorBadgeCount %>
            </span>
          <% } %>
        </a>

        <a class="nav-item <%= activeMenu === 'escala' ? 'active' : '' %>" href="/escala">Escala</a>

        <div class="nav-divider"></div>
        <div class="nav-section">ADMIN</div>
        <a class="nav-item <%= activeMenu === 'usuarios' ? 'active' : '' %>" href="/usuarios">Gerenciar Usuários</a>
      </nav>

      <div class="sidebar-footer">
        © 2026 Campo do Gado
      </div>
    </aside>

    <main class="main">
      <header class="topbar">
        <div>
          <div class="topbar-title">Manutenção Campo do Gado</div>
          <div class="topbar-sub">Sistema Industrial V2</div>
        </div>

        <div class="topbar-right">
          <% if (user) { %>
            <div class="pill">
              <div class="pill-name"><%= user.nome || 'Usuário' %></div>
              <div class="pill-role"><%= user.role || 'USER' %></div>
            </div>
            <a class="btn btn-ghost btn-sm" href="/logout">Sair</a>
          <% } %>
        </div>
      </header>

      <% if (flash && flash.length) { %>
        <div class="flash">
          <% flash.forEach(f => { %>
            <div class="alert <%= f.type === 'error' ? 'alert-error' : 'alert-success' %>"><%= f.message %></div>
          <% }) %>
        </div>
      <% } %>

      <div class="content">
        <%- body %>
      </div>

      <footer class="footer">
        <span>Manutenção Industrial • Campo do Gado</span>
        <span>© 2026 • Sistema V2</span>
      </footer>
    </main>
  </div>
</body>
</html>
